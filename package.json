{
  "name": "miles",
  "version": "1.0.0",
  "description": "Miles - Data augmentation and reward platform",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "start": "cd apps/web && npm start",
    "lint": "cd apps/web && npm run lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

